{
  "name": "viewbound",
  "version": "0.1.0",
  "description": "Curated editorial platform for narrative travel content",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand",
    "migrate": "sequelize-cli db:migrate",
    "migrate:undo": "sequelize-cli db:migrate:undo"
  },
  "dependencies": {
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "sequelize-cli": "^6.6.2",
    "sqlite3": "^5.1.7"
  }
}
